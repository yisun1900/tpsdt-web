package com.sh.tpsdt.model.brute_meter;

public interface BruteMeterStandard {

    Double SUPER_LOW = Math.pow(10, 8);

    Double LOW = Math.pow(10, 10);

    Double MEDIUM = Math.pow(10, 12);

    Double HIGH = Math.pow(10, 14);

    Double SUPER_HIGH = Math.pow(10, 14);
}
